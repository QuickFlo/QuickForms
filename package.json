{
  "name": "@quickforms/monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic JSON Schema form generator with first-class Quasar support",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "keywords": [
    "jsonschema",
    "forms",
    "vue",
    "quasar",
    "validation",
    "dynamic-forms"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
